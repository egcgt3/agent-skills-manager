import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Edit Skill | Agent Skills Manager",
};

export default function EditSkillLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}

import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Create Skill | Agent Skills Manager",
};

export default function NewSkillLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
